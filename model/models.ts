import localVarRequest from 'request';

export * from './apiAccountsGetProfileIdGet200Response';
export * from './apiAccountsGetProfileIdGet401Response';
export * from './apiAccountsRegisterPost200Response';
export * from './apiAccountsRegisterPost401Response';
export * from './apiAccountsRegisterPost405Response';
export * from './apiProfilesGetGet200Response';
export * from './apiProfilesGetGet401Response';
export * from './apiProfilesInviteGet200Response';
export * from './apiProfilesInviteGet400Response';
export * from './apiProfilesIsCompanyGet200Response';
export * from './apiProfilesNewPost200Response';
export * from './apiProfilesNewPost401Response';
export * from './apiSessionCreatePost200Response';
export * from './apiSessionCreatePost401Response';
export * from './apiSessionDestroyPost200Response';
export * from './apiSessionDestroyPost401Response';
export * from './apiSessionDestroyPost405Response';
export * from './apiSessionVerifyGet200Response';

import * as fs from 'fs';

export interface RequestDetailedFile {
    value: Buffer;
    options?: {
        filename?: string;
        contentType?: string;
    }
}

export type RequestFile = string | Buffer | fs.ReadStream | RequestDetailedFile;


import { ApiAccountsGetProfileIdGet200Response } from './apiAccountsGetProfileIdGet200Response';
import { ApiAccountsGetProfileIdGet401Response } from './apiAccountsGetProfileIdGet401Response';
import { ApiAccountsRegisterPost200Response } from './apiAccountsRegisterPost200Response';
import { ApiAccountsRegisterPost401Response } from './apiAccountsRegisterPost401Response';
import { ApiAccountsRegisterPost405Response } from './apiAccountsRegisterPost405Response';
import { ApiProfilesGetGet200Response } from './apiProfilesGetGet200Response';
import { ApiProfilesGetGet401Response } from './apiProfilesGetGet401Response';
import { ApiProfilesInviteGet200Response } from './apiProfilesInviteGet200Response';
import { ApiProfilesInviteGet400Response } from './apiProfilesInviteGet400Response';
import { ApiProfilesIsCompanyGet200Response } from './apiProfilesIsCompanyGet200Response';
import { ApiProfilesNewPost200Response } from './apiProfilesNewPost200Response';
import { ApiProfilesNewPost401Response } from './apiProfilesNewPost401Response';
import { ApiSessionCreatePost200Response } from './apiSessionCreatePost200Response';
import { ApiSessionCreatePost401Response } from './apiSessionCreatePost401Response';
import { ApiSessionDestroyPost200Response } from './apiSessionDestroyPost200Response';
import { ApiSessionDestroyPost401Response } from './apiSessionDestroyPost401Response';
import { ApiSessionDestroyPost405Response } from './apiSessionDestroyPost405Response';
import { ApiSessionVerifyGet200Response } from './apiSessionVerifyGet200Response';

/* tslint:disable:no-unused-variable */
let primitives = [
                    "string",
                    "boolean",
                    "double",
                    "integer",
                    "long",
                    "float",
                    "number",
                    "any"
                 ];

let enumsMap: {[index: string]: any} = {
}

let typeMap: {[index: string]: any} = {
    "ApiAccountsGetProfileIdGet200Response": ApiAccountsGetProfileIdGet200Response,
    "ApiAccountsGetProfileIdGet401Response": ApiAccountsGetProfileIdGet401Response,
    "ApiAccountsRegisterPost200Response": ApiAccountsRegisterPost200Response,
    "ApiAccountsRegisterPost401Response": ApiAccountsRegisterPost401Response,
    "ApiAccountsRegisterPost405Response": ApiAccountsRegisterPost405Response,
    "ApiProfilesGetGet200Response": ApiProfilesGetGet200Response,
    "ApiProfilesGetGet401Response": ApiProfilesGetGet401Response,
    "ApiProfilesInviteGet200Response": ApiProfilesInviteGet200Response,
    "ApiProfilesInviteGet400Response": ApiProfilesInviteGet400Response,
    "ApiProfilesIsCompanyGet200Response": ApiProfilesIsCompanyGet200Response,
    "ApiProfilesNewPost200Response": ApiProfilesNewPost200Response,
    "ApiProfilesNewPost401Response": ApiProfilesNewPost401Response,
    "ApiSessionCreatePost200Response": ApiSessionCreatePost200Response,
    "ApiSessionCreatePost401Response": ApiSessionCreatePost401Response,
    "ApiSessionDestroyPost200Response": ApiSessionDestroyPost200Response,
    "ApiSessionDestroyPost401Response": ApiSessionDestroyPost401Response,
    "ApiSessionDestroyPost405Response": ApiSessionDestroyPost405Response,
    "ApiSessionVerifyGet200Response": ApiSessionVerifyGet200Response,
}

// Check if a string starts with another string without using es6 features
function startsWith(str: string, match: string): boolean {
    return str.substring(0, match.length) === match;
}

// Check if a string ends with another string without using es6 features
function endsWith(str: string, match: string): boolean {
    return str.length >= match.length && str.substring(str.length - match.length) === match;
}

const nullableSuffix = " | null";
const optionalSuffix = " | undefined";
const arrayPrefix = "Array<";
const arraySuffix = ">";
const mapPrefix = "{ [key: string]: ";
const mapSuffix = "; }";

export class ObjectSerializer {
    public static findCorrectType(data: any, expectedType: string) {
        if (data == undefined) {
            return expectedType;
        } else if (primitives.indexOf(expectedType.toLowerCase()) !== -1) {
            return expectedType;
        } else if (expectedType === "Date") {
            return expectedType;
        } else {
            if (enumsMap[expectedType]) {
                return expectedType;
            }

            if (!typeMap[expectedType]) {
                return expectedType; // w/e we don't know the type
            }

            // Check the discriminator
            let discriminatorProperty = typeMap[expectedType].discriminator;
            if (discriminatorProperty == null) {
                return expectedType; // the type does not have a discriminator. use it.
            } else {
                if (data[discriminatorProperty]) {
                    var discriminatorType = data[discriminatorProperty];
                    if(typeMap[discriminatorType]){
                        return discriminatorType; // use the type given in the discriminator
                    } else {
                        return expectedType; // discriminator did not map to a type
                    }
                } else {
                    return expectedType; // discriminator was not present (or an empty string)
                }
            }
        }
    }

    public static serialize(data: any, type: string): any {
        if (data == undefined) {
            return data;
        } else if (primitives.indexOf(type.toLowerCase()) !== -1) {
            return data;
        } else if (endsWith(type, nullableSuffix)) {
            let subType: string = type.slice(0, -nullableSuffix.length); // Type | null => Type
            return ObjectSerializer.serialize(data, subType);
        } else if (endsWith(type, optionalSuffix)) {
            let subType: string = type.slice(0, -optionalSuffix.length); // Type | undefined => Type
            return ObjectSerializer.serialize(data, subType);
        } else if (startsWith(type, arrayPrefix)) {
            let subType: string = type.slice(arrayPrefix.length, -arraySuffix.length); // Array<Type> => Type
            let transformedData: any[] = [];
            for (let index = 0; index < data.length; index++) {
                let datum = data[index];
                transformedData.push(ObjectSerializer.serialize(datum, subType));
            }
            return transformedData;
        } else if (startsWith(type, mapPrefix)) {
            let subType: string = type.slice(mapPrefix.length, -mapSuffix.length); // { [key: string]: Type; } => Type
            let transformedData: { [key: string]: any } = {};
            for (let key in data) {
                transformedData[key] = ObjectSerializer.serialize(
                    data[key],
                    subType,
                );
            }
            return transformedData;
        } else if (type === "Date") {
            return data.toISOString();
        } else {
            if (enumsMap[type]) {
                return data;
            }
            if (!typeMap[type]) { // in case we dont know the type
                return data;
            }

            // Get the actual type of this object
            type = this.findCorrectType(data, type);

            // get the map for the correct type.
            let attributeTypes = typeMap[type].getAttributeTypeMap();
            let instance: {[index: string]: any} = {};
            for (let index = 0; index < attributeTypes.length; index++) {
                let attributeType = attributeTypes[index];
                instance[attributeType.baseName] = ObjectSerializer.serialize(data[attributeType.name], attributeType.type);
            }
            return instance;
        }
    }

    public static deserialize(data: any, type: string): any {
        // polymorphism may change the actual type.
        type = ObjectSerializer.findCorrectType(data, type);
        if (data == undefined) {
            return data;
        } else if (primitives.indexOf(type.toLowerCase()) !== -1) {
            return data;
        } else if (endsWith(type, nullableSuffix)) {
            let subType: string = type.slice(0, -nullableSuffix.length); // Type | null => Type
            return ObjectSerializer.deserialize(data, subType);
        } else if (endsWith(type, optionalSuffix)) {
            let subType: string = type.slice(0, -optionalSuffix.length); // Type | undefined => Type
            return ObjectSerializer.deserialize(data, subType);
        } else if (startsWith(type, arrayPrefix)) {
            let subType: string = type.slice(arrayPrefix.length, -arraySuffix.length); // Array<Type> => Type
            let transformedData: any[] = [];
            for (let index = 0; index < data.length; index++) {
                let datum = data[index];
                transformedData.push(ObjectSerializer.deserialize(datum, subType));
            }
            return transformedData;
        } else if (startsWith(type, mapPrefix)) {
            let subType: string = type.slice(mapPrefix.length, -mapSuffix.length); // { [key: string]: Type; } => Type
            let transformedData: { [key: string]: any } = {};
            for (let key in data) {
                transformedData[key] = ObjectSerializer.deserialize(
                    data[key],
                    subType,
                );
            }
            return transformedData;
        } else if (type === "Date") {
            return new Date(data);
        } else {
            if (enumsMap[type]) {// is Enum
                return data;
            }

            if (!typeMap[type]) { // dont know the type
                return data;
            }
            let instance = new typeMap[type]();
            let attributeTypes = typeMap[type].getAttributeTypeMap();
            for (let index = 0; index < attributeTypes.length; index++) {
                let attributeType = attributeTypes[index];
                instance[attributeType.name] = ObjectSerializer.deserialize(data[attributeType.baseName], attributeType.type);
            }
            return instance;
        }
    }
}

export interface Authentication {
    /**
    * Apply authentication settings to header and query params.
    */
    applyToRequest(requestOptions: localVarRequest.Options): Promise<void> | void;
}

export class HttpBasicAuth implements Authentication {
    public username: string = '';
    public password: string = '';

    applyToRequest(requestOptions: localVarRequest.Options): void {
        requestOptions.auth = {
            username: this.username, password: this.password
        }
    }
}

export class HttpBearerAuth implements Authentication {
    public accessToken: string | (() => string) = '';

    applyToRequest(requestOptions: localVarRequest.Options): void {
        if (requestOptions && requestOptions.headers) {
            const accessToken = typeof this.accessToken === 'function'
                            ? this.accessToken()
                            : this.accessToken;
            requestOptions.headers["Authorization"] = "Bearer " + accessToken;
        }
    }
}

export class ApiKeyAuth implements Authentication {
    public apiKey: string = '';

    constructor(private location: string, private paramName: string) {
    }

    applyToRequest(requestOptions: localVarRequest.Options): void {
        if (this.location == "query") {
            (<any>requestOptions.qs)[this.paramName] = this.apiKey;
        } else if (this.location == "header" && requestOptions && requestOptions.headers) {
            requestOptions.headers[this.paramName] = this.apiKey;
        } else if (this.location == 'cookie' && requestOptions && requestOptions.headers) {
            if (requestOptions.headers['Cookie']) {
                requestOptions.headers['Cookie'] += '; ' + this.paramName + '=' + encodeURIComponent(this.apiKey);
            }
            else {
                requestOptions.headers['Cookie'] = this.paramName + '=' + encodeURIComponent(this.apiKey);
            }
        }
    }
}

export class OAuth implements Authentication {
    public accessToken: string = '';

    applyToRequest(requestOptions: localVarRequest.Options): void {
        if (requestOptions && requestOptions.headers) {
            requestOptions.headers["Authorization"] = "Bearer " + this.accessToken;
        }
    }
}

export class VoidAuth implements Authentication {
    public username: string = '';
    public password: string = '';

    applyToRequest(_: localVarRequest.Options): void {
        // Do nothing
    }
}

export type Interceptor = (requestOptions: localVarRequest.Options) => (Promise<void> | void);
